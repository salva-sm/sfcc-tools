[package]
name = "sfcc-core"
version = "0.1.0"
edition = "2024"
license.workspace = true
repository.workspace = true
authors = ["salva-sm"]
description = "What every SFCC tool here needs: the sandbox credentials in dw.json, and what may be written to"

[dependencies]
anyhow = "1.0.104"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
