name: Docs

# `cargo doc` is the reference for the language server's internals; publishing
# it means a reader never has to clone the repository to read one.
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# A queued run supersedes the one waiting, but never cancels one mid-deploy.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # `-D warnings` keeps a broken intra-doc link from shipping as a dead
      # link on the published site.
      - name: Build the documentation
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --no-deps --lib -p isml-lsp

      # Pages serves the directory root, and rustdoc puts the crate one level
      # down; without this the site opens on a file listing.
      - name: Redirect the root to the crate
        run: echo '<meta http-equiv="refresh" content="0;url=isml_lsp/index.html">' > target/doc/index.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: target/doc

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@v4
